[project]
name = "todo-voice-agent"
version = "1.0.0"
description = "Teach your to-do list to listen — a beginner workshop for the Deepgram Voice Agent API, in Python"
requires-python = ">=3.10"
dependencies = [
  "deepgram-sdk==7.8.0",
  "aiohttp>=3.9,<4",
]

[tool.uv]
package = false
